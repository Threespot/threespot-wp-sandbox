{{--
  Notes:
  - Only add additional skip links when deemed necessary.
    For example, a “skip to nav” link isn’t necessary if the nav
    nav immediately follows the skip links in the tabbing order.
  - A “skip to footer” link may help keyboard users who don’t use
    a screenreader (e.g. mobility issues). It’s also helpful
    if the footer contains important links that aren’t found in
    the main nav.
  - https://webaim.org/techniques/skipnav/
  - https://www.a11yproject.com/posts/skip-nav-links/
  - http://www.456bereastreet.com/archive/200909/skip_links_need_to_be_at_least_temporarily_visible/
--}}
<a class="SkipLink" href="#title">Skip to content</a>
{{-- <a class="SkipLink" href="#footer">Skip to footer</a> --}}
